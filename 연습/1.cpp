#include <string>
#include <vector>
#include <map>

using namespace std;

void search_possible()
{
}

// 전역 변수를 정의할 경우 함수 내에 초기화 코드를 꼭 작성해주세요.
string solution(int m, int n, vector<string> board)
{
    string answer = "";
    vector<vector<char>> map;
    for (int i = 0; i < board.size(); i++)
    {
        for (int j = 0; j < board[i].size(); j++)
        {
        }
    }
    return answer;
}